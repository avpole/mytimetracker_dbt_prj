version: 2

models:

  - name: int_person_activities
    description: person desktop activities
    columns:
      - name: activity_id
        description: primary key
        tests:
          - not_null
          - unique
      - name: tracktime
        description: Datetime
        tests:
          - not_null
      - name: processfilepath
        description: string
        tests:
          - not_null