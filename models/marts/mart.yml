version: 2

models:

  - name: mart_person_activity_hour_facts
    description: person desktop hourly activities facts 
    columns:
      - name: activity_id
        description: surrogate key
        tests:
          - not_null
          - unique
      - name: activity_hour
        description: Datetime
        tests:
          - not_null